// 从 env.ts 导入，保持一致性
export {
  SAVE_DICT_KEY,
  SAVE_SETTING_KEY,
  APP_VERSION,
  LOCAL_FILE_KEY,
  DICT_LIST,
  SoundFileOptions,
  TourConfig,
} from '@/config/env'

export const TOKEN_KEY = 'token' // 保持与原项目一致
